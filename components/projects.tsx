import React from 'react'

import { CardInfo } from './info/CardInfo'

type Props = {}

function projects({ }: Props) {

    //create a function to create cards and project card based on json

    //the function looks at the cards folder and returns an array of htmx elements


    return (
        <div>
            projects


        </div>
    )
}

export default projects
